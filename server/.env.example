# Copia a .env y rellena. Si DATABASE_URL queda vacío, se usa PGlite local (server/.pgdata) — cero setup.
DATABASE_URL=
# Neon (recomendado GCP): postgresql://user:pass@ep-xxx.neon.tech/db?sslmode=require
# Cloud SQL: postgresql://user:pass@/db?host=/cloudsql/PROJECT:REGION:INSTANCE

JWT_SECRET=cambia-esto-por-una-cadena-larga-y-secreta
OPENAI_API_KEY=sk-...
PORT=8080
